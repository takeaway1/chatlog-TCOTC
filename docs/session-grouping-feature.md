# 会话分组功能说明

## 概述

本功能为 Chatlog 项目添加了微信会话的智能分组显示功能，支持：

1. **置顶会话** - 显示在最上方，支持折叠
2. **普通会话** - 显示在中间，正常排列
3. **最小化会话** - 合并显示，点击展开

所有折叠/展开状态通过 localStorage 持久化保存。

## 后端实现

### 数据模型扩展

在 `internal/model/session.go` 中，`Session` 结构体新增了三个字段：

```go
type Session struct {
    // ... 原有字段 ...

    // 新增字段
    IsTopPinned  bool   `json:"isTopPinned,omitempty"`  // 是否置顶
    IsHidden     bool   `json:"isHidden,omitempty"`     // 是否最小化
    SortOrder    int64  `json:"sortOrder,omitempty"`    // 排序时间戳
}
```

### WeChat 4.x 支持

**检测逻辑** (`internal/model/session_v4.go`):

```go
// 置顶判断：使用 status 字段
// status=0: 普通会话
// status=2: 置顶会话
// status=4: 置顶会话（可能带免打扰状态）
isTopPinned := s.Status == 2 || s.Status == 4

// 最小化判断：is_hidden 字段
isHidden := s.IsHidden == 1
```

**数据库字段**:
- `status` - 会话状态（0=普通，2=置顶，4=置顶+其他状态）
- `is_hidden` - 最小化标记（1=已最小化）
- `sort_timestamp` - 排序时间戳（用于同类会话内部排序）

**排序规则**:
```sql
ORDER BY
  CASE WHEN status IN (2, 4) THEN 0 ELSE 1 END,  -- 置顶优先
  sort_timestamp DESC                             -- 时间倒序
```

这确保所有置顶会话始终排在最前面，不受最后消息时间影响。

### WeChat 3.x 支持

**字段**:
- `nOrder` - 排序序号（用作 sortOrder）
- `parentRef` - 可能用于分组标记（待验证）

**注意**: WeChat 3.x 没有 `is_hidden` 字段，需要通过实际数据分析 `parentRef` 的含义。

## 前端实现

### API 类型定义

`web/src/libs/ChatlogAPI.ts`:

```typescript
export interface Session {
  userName: string;
  nOrder: number;
  parentRef: string;
  nickName: string;
  content: string;
  nTime: string;
  nUnReadCount: number;
  avatarUrl?: string;

  // 新增字段
  isTopPinned?: boolean;
  isHidden?: boolean;
  sortOrder?: number;
}
```

### 组件使用

引入新的 `EnhancedSessionList` 组件：

```tsx
import { EnhancedSessionList } from '@/components/chatlog/EnhancedSessionList';

export default function SessionsPage() {
  return <EnhancedSessionList />;
}
```

### 功能特性

1. **自动分组**
   - 置顶会话：蓝色背景卡片，显示 📌 图标
   - 普通会话：默认样式
   - 最小化会话：灰色边框，初始折叠

2. **交互功能**
   - 点击置顶区域标题：折叠/展开置顶会话
   - 点击最小化区域标题：展开/折叠最小化会话
   - 状态自动保存到 localStorage

3. **持久化存储**
   - `chatlog_pinned_collapsed` - 置顶区域折叠状态
   - `chatlog_minimized_expanded` - 最小化区域展开状态

## 使用示例

### 后端 API 响应

```json
{
  "items": [
    {
      "userName": "user123",
      "nickName": "张三",
      "content": "最近怎么样？",
      "nTime": "2025-11-09T12:00:00+08:00",
      "nUnReadCount": 5,
      "isTopPinned": true,
      "isHidden": false,
      "sortOrder": 9999999999
    },
    {
      "userName": "user456",
      "nickName": "李四",
      "content": "好的",
      "nTime": "2025-11-09T11:00:00+08:00",
      "nUnReadCount": 0,
      "isTopPinned": false,
      "isHidden": false,
      "sortOrder": 1731127200
    },
    {
      "userName": "user789",
      "nickName": "王五",
      "content": "知道了",
      "nTime": "2025-11-08T10:00:00+08:00",
      "nUnReadCount": 0,
      "isTopPinned": false,
      "isHidden": true,
      "sortOrder": 1731040800
    }
  ],
  "total": 3
}
```

### 前端显示效果

```
┌─────────────────────────────────┐
│ 📌 置顶会话 [2]           ▼     │
├─────────────────────────────────┤
│ 📌 张三  12:00  [5条未读]      │
│    最近怎么样？                  │
│                                 │
│ 📌 赵六  11:30                  │
│    明天见                        │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ 💬 普通会话 [3]                 │
├─────────────────────────────────┤
│ 李四  11:00                      │
│    好的                          │
│                                 │
│ 孙七  10:30                      │
│    收到                          │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ ➖ 最小化会话 [2]          ▶     │
└─────────────────────────────────┘
```

## 技术细节

### 置顶判断逻辑

WeChat 4.x 使用 `status` 字段标识会话状态：

```go
// 使用 status 字段判断
isTopPinned := status == 2 || status == 4
```

**状态值说明**：
- `status=0`: 普通会话（664个）
- `status=2`: 置顶会话（15个）
- `status=4`: 置顶+其他状态，如免打扰（3个）

**关键发现**：
- ❌ `sort_timestamp` **不是**置顶标识（只用于排序）
- ✅ `status` 字段才是真正的置顶标识
- 置顶会话的 `sort_timestamp` 可能比普通会话更旧

**为什么需要特殊排序**：
由于置顶会话的 `sort_timestamp` 可能很旧（例如最后消息是几天前），如果只按 `sort_timestamp DESC` 排序，这些置顶会话会被排到很后面。因此需要先按 `status` 分组，再按时间排序：

```sql
ORDER BY
  CASE WHEN status IN (2, 4) THEN 0 ELSE 1 END,  -- 置顶会话优先级0，普通会话优先级1
  sort_timestamp DESC                             -- 同优先级内按时间倒序
```

### 排序规则

**后端排序**（SQL）：
1. 第一优先级：`status IN (2,4)` 的置顶会话排在前面
2. 第二优先级：同类会话按 `sort_timestamp DESC` 排序
3. 结果：所有置顶会话在前，按时间倒序；普通会话在后，也按时间倒序

**前端分组**（React）：
1. 将所有会话按 `isTopPinned` 和 `isHidden` 分为三组
2. 置顶组：`isTopPinned === true`
3. 普通组：`isTopPinned === false && isHidden === false`
4. 最小化组：`isHidden === true`
5. 每组内部保持后端返回的顺序

### 性能优化

- 使用 `useMemo` 缓存分组结果
- localStorage 状态仅在变化时更新
- 折叠状态不影响数据加载

## 待完善功能

1. **WeChat 3.x 置顶检测**
   - 需要分析实际数据中 `parentRef` 的值
   - 可能需要额外的阈值判断逻辑

2. **macOS 版本支持**
   - 需要测试 Darwin V3 版本的数据结构
   - 可能需要调整 `SessionDarwinV3` 的 Wrap 函数

3. **动态阈值配置**
   - 将置顶判断阈值设为可配置项
   - 支持不同版本的自定义阈值

4. **测试覆盖**
   - 添加单元测试验证分组逻辑
   - E2E 测试验证前端交互

## 相关文件

### 后端
- `internal/model/session.go` - Session 模型定义
- `internal/model/session_v4.go` - WeChat 4.x 适配
- `internal/wechatdb/datasource/v4/datasource.go` - V4 数据源
- `internal/wechatdb/datasource/windowsv3/datasource.go` - V3 数据源

### 前端
- `web/src/libs/ChatlogAPI.ts` - API 类型定义
- `web/src/components/chatlog/EnhancedSessionList.tsx` - 增强会话列表组件

## 贡献指南

如果需要修改分组逻辑或添加新功能：

1. 后端修改 `SessionV4.Wrap()` 或 `SessionV3.Wrap()` 函数
2. 前端修改 `EnhancedSessionList` 组件的 `groupedSessions` useMemo
3. 更新相关测试和文档
